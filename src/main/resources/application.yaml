spring:
  application:
    name: shop-management-api

  datasource:
    url: jdbc:postgresql://localhost:5433/shop_management
    username: managementuser
    password: management1234

  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: true

server:
  port: 8081
